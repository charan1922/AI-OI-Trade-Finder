/** Trade Assistant — barrel exports. Server-only (imports Node + Azure SDK). */
export { runAssistant } from './assistant';
export { hasAzureConfig } from './azure-client';
export type { AssistantResult, ChatMessage, ToolTraceEntry } from './types';
