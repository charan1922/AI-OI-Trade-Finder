# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# typescript
*.tsbuildinfo
next-env.d.ts

# local database (project-r.db tracked via Git LFS)
data/.dhan-token.json
data/.fyers-token.json
data/project-r.db

# python venv (notebooks/eda.ipynb — local-only, not committed)
.venv/
.ipynb_checkpoints/
__pycache__/
.claude/settings.json
data/parquet/
data/project-r.db-wal
data/project-r.db-shm

# commentary replay-bench run artifacts (regenerable via scripts/replay-commentary.ts)
data/replay-commentary/

# pulled prod secrets — never commit
prod.env
*.env.raw
